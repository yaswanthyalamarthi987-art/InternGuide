<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>InternGuide - Upload Resume</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">

<div class="container mt-5 mb-5">

    <!-- Page Title -->
    <h2 class="text-center mb-4">InternGuide – Resume Analysis & Career Guidance</h2>

    <!-- Message Alert -->
    {% if message %}
        <div class="alert alert-success text-center">
            {{ message }}
        </div>
    {% endif %}

    <!-- Upload Card -->
    <div class="card shadow-sm p-4 mb-4">
        <h4 class="mb-3">📤 Upload Resume (PDF)</h4>
        <form method="POST" enctype="multipart/form-data">
            <div class="mb-3">
                <input type="file" name="resume" class="form-control" required>
            </div>
            <button type="submit" class="btn btn-primary w-100">
                Upload & Analyze
            </button>
        </form>
    </div>

    <!-- Resume Preview -->
    {% if resume_text %}
    <div class="card shadow-sm p-4 mb-4">
        <h4>📄 Extracted Resume Text (Preview)</h4>
        <pre class="bg-light p-3 border rounded" style="max-height:300px; overflow:auto;">
{{ resume_text }}
        </pre>
    </div>
    {% endif %}

    <!-- AI Resume Feedback -->
    {% if ai_feedback %}
    <div class="card shadow-sm p-4 mb-4">
        <h4>🤖 AI Resume Feedback</h4>

        <strong>Skills</strong>
        <ul>
            {% for skill in ai_feedback.skills %}
                <li>{{ skill }}</li>
            {% endfor %}
        </ul>

        <strong>Strengths</strong>
        <ul>
            {% for s in ai_feedback.strengths %}
                <li>{{ s }}</li>
            {% endfor %}
        </ul>

        <strong>Weaknesses</strong>
        <ul>
            {% for w in ai_feedback.weaknesses %}
                <li>{{ w }}</li>
            {% endfor %}
        </ul>

        <strong>Suggestions</strong>
        <ul>
            {% for sug in ai_feedback.suggestions %}
                <li>{{ sug }}</li>
            {% endfor %}
        </ul>
    </div>
    {% endif %}

    <!-- Detected Skills -->
    {% if detected_skills %}
    <div class="card shadow-sm p-4 mb-4">
        <h4>🛠 Detected Skills</h4>
        <ul>
            {% for skill in detected_skills %}
                <li>{{ skill }}</li>
            {% endfor %}
        </ul>
    </div>
    {% endif %}

    <!-- Internship Recommendations -->
    {% if internship_recommendations %}
    <div class="card shadow-sm p-4 mb-4">
        <h4>🎯 Recommended Internships / Jobs</h4>
        <ul>
            {% for role in internship_recommendations %}
                <li>{{ role }}</li>
            {% endfor %}
        </ul>
    </div>
    {% endif %}

</div>

</body>
</html>
